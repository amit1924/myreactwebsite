import React from 'react';
import ReactDOM  from 'react-dom';
import App from './App';
import './index.css'


ReactDOM.render(<App />, document.getElementById('root'));  //it will render insdie index.html which is inside the public folder